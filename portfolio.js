document.addEventListener('DOMContentLoaded',()=>{const portfolioTabs=document.querySelectorAll('.portfolio-tab');const portfolioItems=document.querySelectorAll('.portfolio-item');if(portfolioTabs.length&&portfolioItems.length){portfolioTabs.forEach(tab=>{tab.addEventListener('click',()=>{portfolioTabs.forEach(t=>t.classList.remove('active'));tab.classList.add('active');const category=tab.getAttribute('data-category');let visibleCount=0;portfolioItems.forEach(item=>{if(category==='all'||item.getAttribute('data-category')===category){item.style.display='flex';visibleCount++}else{item.style.display='none'}});const portfolioGrid=document.querySelector('.portfolio-grid');document.querySelectorAll('.portfolio-placeholder').forEach(placeholder=>{placeholder.remove()});if(visibleCount%4!==0&&visibleCount>0){const placeholdersNeeded=4-(visibleCount%4);for(let i=0;i<placeholdersNeeded;i++){const placeholder=document.createElement('div');placeholder.className='portfolio-placeholder';placeholder.style.visibility='hidden';portfolioGrid.appendChild(placeholder)}}})})}
if(portfolioTabs.length>0&&!document.querySelector('.portfolio-tab.active')){portfolioTabs[0].classList.add('active')}
portfolioTabs.forEach(tab=>{tab.addEventListener('click',function(){const navContainer=this.closest('.portfolio-nav');if(navContainer){const tabRect=this.getBoundingClientRect();const containerRect=navContainer.getBoundingClientRect();const scrollPosition=navContainer.scrollLeft+(tabRect.left-containerRect.left)-(containerRect.width/2)+(tabRect.width/2);navContainer.scrollTo({left:scrollPosition,behavior:'smooth'})}})})})